# Core
websockets>=12.0
httpx>=0.27
PyYAML>=6.0
# X access is provided by the API server's Replit connector proxy; no
# credential-bearing X client is installed in this Python process.

# Telegram
python-telegram-bot>=21.0

# Regression validation (no wallet or network access)
pytest>=8.0

# Polymarket real-mode trading (only needed for `mode: real`)
# Install separately if you actually want to place real orders:
#   pip install py-clob-client-v2
py-clob-client-v2>=1.1.0 ; sys_platform != "win32" or python_version < "3.13"
